// Package auth is resleeve's identity and encryption subsystem.
// Argon2id password hashing; KEK two-wrap (password + recovery key);
// KeyStore abstraction over OS keychains.
// See docs/design/round-2/10-auth-subsystem.md.
package auth
